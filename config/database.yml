default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: book_bot
  username: postgres
  password: password
  host: db # This matches the service name in docker-compose.yml