{
  "name": "book-cover-bot",
  "version": "1.0.0",
  "engines": {
    "node": ">=18.x",
    "npm": ">=9.x"
  },
  "scripts": {
    "build": "cd bookbot-frontend && npm install && npm run build",
    "heroku-postbuild": "npm run build && cp -r bookbot-frontend/build/* bookbot-api/public/ && cp -r bookbot-api/Gemfile* ."
  }
}
